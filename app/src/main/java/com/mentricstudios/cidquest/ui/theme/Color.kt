package com.mentricstudios.cidquest.ui.theme

import androidx.compose.ui.graphics.Color

// Navy & Gold "detective badge" palette for Cid Quest
val BackgroundTop = Color(0xFF0B1330)      // Deep navy
val BackgroundBottom = Color(0xFF05081A)   // Near-black navy, deeper gradient stop
val AccentGold = Color(0xFFE3B94D)         // Brass gold — primary accent / hero glow
val AccentAmber = Color(0xFFD98A3D)        // Antique amber — secondary accent / stars & rewards
val TextPrimary = Color(0xFFF4EEDD)        // Warm parchment-white
val TextSecondary = Color(0xFF8792AC)      // Muted slate blue-grey

val CardLocked = Color(0xFF12162E)
val LockGrey = Color(0xFF55607F)

// Enemies — the only mode in the game. The goal ring, win particles, and
// the patrol guards themselves all share this "wanted poster" crimson, so
// the whole board reads as one consistent case/danger palette.
val CategoryEnemies = Color(0xFFC1272D)
val EnemyColor = Color(0xFFC1272D)
